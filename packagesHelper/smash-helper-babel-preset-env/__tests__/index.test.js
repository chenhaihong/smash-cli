'use strict';

const smashHelperBabelPresetEnv = require('../lib');

describe('smash-helper-babel-preset-env', () => {
  it('needs tests', function() {
    smashHelperBabelPresetEnv();
    expect(1).toBe(1);
  });
});
