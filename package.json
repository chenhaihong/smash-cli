{
  "name": "smash-cli-monorepo",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "eslint --ext .js packages --fix",
    "test": "lerna exec test"
  },
  "devDependencies": {
    "eslint": "^6.0.0",
    "jest": "^24.8.0"
  }
}
