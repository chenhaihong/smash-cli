{
  "private": true,
  "name": "smash-cli-monorepo",
  "version": "0.0.6",
  "scripts": {
    "eslint": "eslint --ext .js --fix ./",
    "prettier": "prettier --check --write ./**/*.js"
  },
  "devDependencies": {
    "eslint": "^6.0.0",
    "husky": "^3.0.0",
    "jest": "^24.8.0",
    "prettier": "1.18.2",
    "pretty-quick": "^1.11.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
