#!/usr/bin/env node
'use strict';

const helloworld = require('.');

describe('smash-middleware-helloworld', () => {
    test('it should run well', () => {
        helloworld({}, {}, null);
    });

    test('it should show desired text', () => {
        helloworld({}, {}, null);
    });
});
